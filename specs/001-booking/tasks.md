# Tasks: จองคิวตรวจสุขภาพ (Booking)

- Feature: จองคิวตรวจสุขภาพ (Booking)
- Spec ID: SPEC-BKG-001
- อ้างอิง: [plan.md](plan.md)
- วันที่: 2569-09-23

สรุป: มีทั้งหมด 19 tasks ครอบคลุมงานข้อมูล, API, หน้าจอ, การเชื่อมต่อ และการทดสอบตาม Acceptance Criteria.
มี 7 tasks ที่ต้องรอ Open Question `Q-02` เรื่องรูปแบบและวิธีออกหมายเลขคิว.

## รายการงาน

### T-01 สร้างโมเดลและ migration ฐานข้อมูล
- รองรับ: CON-TECH-01, IF-HIS-01, DOM-PDPA-01
- ตรวจด้วย: ไม่มี AC ตรง ๆ เป็นงานพื้นฐานของ T-03, T-10 และ T-11
- ไฟล์ที่แตะ: `backend/app/config.py`, `backend/app/db/models.py`, `backend/app/db/session.py`, `backend/app/db/migrations/001_init.py`, `backend/tests/conftest.py`
- ต้องทำหลัง: ไม่มี
- เสร็จเมื่อ: migration สร้างตาราง `slots`, `bookings` และ `audit_logs` ได้ และตาราง `bookings` ไม่มีคอลัมน์เลขบัตรประชาชน
- สถานะ: พร้อมทำ

### T-02 ตรวจผลยืนยันตัวตนก่อนเข้าถึงข้อมูล
- รองรับ: IF-IDP-01
- ตรวจด้วย: ไม่มี AC ตรง ๆ เป็นงานพื้นฐานของ T-04, T-05, T-09 และ T-11
- ไฟล์ที่แตะ: `backend/app/auth/idp.py`, `backend/app/main.py`
- ต้องทำหลัง: ไม่มี
- เสร็จเมื่อ: endpoint ที่แตะข้อมูลผู้รับบริการปฏิเสธคำขอที่ไม่มีผลยืนยันตัวตน และยอมให้คำขอที่ยืนยันแล้วผ่านตามสัญญา API
- สถานะ: พร้อมทำ

### T-03 สร้างบริการค้นหา HN จาก HIS
- รองรับ: IF-HIS-01
- ตรวจด้วย: ไม่มี AC ตรง ๆ เป็นงานพื้นฐานของ T-04 และ T-09
- ไฟล์ที่แตะ: `backend/app/his/client.py`, `backend/app/booking/router.py`, `backend/app/main.py`, `backend/tests/test_his_lookup.py`
- ต้องทำหลัง: T-01, T-02
- เสร็จเมื่อ: `GET /patients/lookup` รับเลขบัตรเพื่อค้น HIS และส่งคืน HN โดยไม่บันทึกเลขบัตรลงฐานข้อมูล
- สถานะ: พร้อมทำ

### T-04 สร้าง API ค้นหาช่วงเวลาว่าง
- รองรับ: FR-BKG-01, FR-BKG-06, ASM-01, ASM-02
- ตรวจด้วย: ไม่มี AC ตรง ๆ เป็นงานพื้นฐานของ T-05 และ T-13
- ไฟล์ที่แตะ: `backend/app/slots/service.py`, `backend/app/slots/router.py`, `backend/app/main.py`, `backend/tests/test_slots.py`
- ต้องทำหลัง: T-01, T-02
- เสร็จเมื่อ: `GET /slots` คืนช่วงเวลาภายใน 30 วันพร้อมที่นั่งคงเหลือ และคำนวณรายการใหม่เมื่อเปลี่ยน `package_code` ตามข้อมูลช่วงเวลา
- สถานะ: พร้อมทำ

### T-05 ทดสอบประสิทธิภาพการค้นหาช่วงเวลา
- รองรับ: NFR-PERF-01
- ตรวจด้วย: AC-BKG-05
- ไฟล์ที่แตะ: `backend/tests/test_AC_BKG_05.py`
- ต้องทำหลัง: T-04
- เสร็จเมื่อ: การทดสอบจำลองผู้ใช้พร้อมกัน 200 คนวัด p95 ของ `GET /slots` และตรวจค่าไม่เกิน 2 วินาที
- สถานะ: พร้อมทำ

### T-06 บันทึกการจองและตัดที่นั่งแบบ transaction
- รองรับ: FR-BKG-04, IF-HIS-01
- ตรวจด้วย: AC-BKG-01
- ไฟล์ที่แตะ: `backend/app/booking/service.py`, `backend/app/booking/router.py`, `backend/app/main.py`, `backend/tests/test_AC_BKG_01.py`
- ต้องทำหลัง: T-01, T-02, T-03
- เสร็จเมื่อ: การจองที่มีที่นั่งว่างถูกบันทึกโดยอ้างอิง HN ตัด `remaining` เป็น 0 เมื่อเหลือที่นั่งสุดท้าย และคืนผลการจองตามสัญญาโดยส่วนเลขคิวรอ Q-02
- สถานะ: รอ Q-02

### T-07 ป้องกันการจองซ้ำในวันเดียวกัน
- รองรับ: FR-BKG-02, ASM-02
- ตรวจด้วย: AC-BKG-02
- ไฟล์ที่แตะ: `backend/app/booking/service.py`, `backend/app/booking/router.py`, `backend/tests/test_AC_BKG_02.py`
- ต้องทำหลัง: T-06
- เสร็จเมื่อ: ผู้รับบริการที่มีคิวซึ่งยังไม่ได้ใช้ในวันเดียวกันถูกปฏิเสธ และผลลัพธ์อ้างอิงหมายเลขคิวเดิมตามคำตอบ Q-02
- สถานะ: รอ Q-02

### T-08 เสนอช่วงเวลาใกล้เคียงเมื่อช่วงเต็ม
- รองรับ: FR-BKG-03, ASM-02
- ตรวจด้วย: AC-BKG-03
- ไฟล์ที่แตะ: `backend/app/slots/service.py`, `backend/app/booking/service.py`, `backend/app/booking/router.py`, `backend/tests/test_AC_BKG_03.py`
- ต้องทำหลัง: T-04, T-06
- เสร็จเมื่อ: เมื่อช่วงถูกจองเต็มก่อนยืนยัน API คืน 409 พร้อมช่วงว่าง 3 รายการที่ใกล้ที่สุดจากวันเดียวกันและวันถัดไป และไม่สร้างการจองซ้อน
- สถานะ: พร้อมทำ

### T-09 วางงานแจ้งเตือนและส่งซ้ำแบบ asynchronous
- รองรับ: FR-BKG-05, IF-NOT-01, NFR-REL-02, ASM-03
- ตรวจด้วย: AC-BKG-04
- ไฟล์ที่แตะ: `backend/app/notify/queue.py`, `backend/app/booking/service.py`, `backend/app/booking/router.py`, `backend/tests/test_AC_BKG_04.py`
- ต้องทำหลัง: T-06
- เสร็จเมื่อ: การยืนยันการจองวางงานแจ้งเตือนโดยไม่รอผลการส่ง และเมื่อส่งไม่สำเร็จมีงาน retry รอบถัดไปภายใน 5 นาทีตาม ASM-03 ขณะที่การจองยังอยู่
- สถานะ: รอ Q-02

### T-10 บันทึก audit log เมื่อเข้าถึงข้อมูล
- รองรับ: DOM-PDPA-01
- ตรวจด้วย: AC-BKG-06
- ไฟล์ที่แตะ: `backend/app/audit/middleware.py`, `backend/app/main.py`, `backend/tests/test_AC_BKG_06.py`
- ต้องทำหลัง: T-01, T-02
- เสร็จเมื่อ: การเปิดดูข้อมูลการจองสร้าง audit log ที่มีผู้เข้าถึง เวลา และ HN และกำหนดการเก็บข้อมูลไม่น้อยกว่า 1 ปี
- สถานะ: พร้อมทำ

### T-11 สร้าง API ดูผลการจอง
- รองรับ: FR-BKG-05, IF-IDP-01, IF-HIS-01
- ตรวจด้วย: ไม่มี AC ตรง ๆ เป็นงานพื้นฐานของ T-16 และ T-18
- ไฟล์ที่แตะ: `backend/app/booking/router.py`, `backend/app/booking/service.py`, `backend/app/main.py`, `backend/tests/test_booking_result.py`
- ต้องทำหลัง: T-01, T-02, T-06, T-10
- เสร็จเมื่อ: `GET /bookings/{id}` คืนรายละเอียดการจองและสถานะการแจ้งเตือน โดยใช้ HN ภายในระบบและบันทึก audit log
- สถานะ: พร้อมทำ

### T-12 ตั้งค่า TLS สำหรับการรับส่งข้อมูลการจอง
- รองรับ: NFR-SEC-01
- ตรวจด้วย: ไม่มี AC ตรง ๆ เป็นงานพื้นฐานของการเปิดใช้งานระบบตาม plan
- ไฟล์ที่แตะ: `backend/app/config.py`, `backend/app/main.py`, `frontend/vite.config.js`
- ต้องทำหลัง: ไม่มี
- เสร็จเมื่อ: การตั้งค่าระบบกำหนดให้ endpoint การจองรับส่งข้อมูลผ่าน TLS 1.2 ขึ้นไปตามสภาพแวดล้อมที่นำไปใช้
- สถานะ: พร้อมทำ

### T-13 สร้างหน้าจอเลือกแพ็กเกจและช่วงเวลา
- รองรับ: FR-BKG-01, FR-BKG-06
- ตรวจด้วย: ไม่มี AC ตรง ๆ เป็นงานพื้นฐานของ T-14 และ T-17
- ไฟล์ที่แตะ: `frontend/src/pages/SlotPicker.jsx`, `frontend/src/api/client.js`, `frontend/src/App.jsx`, `frontend/src/__tests__/SlotPicker.test.jsx`
- ต้องทำหลัง: ไม่มี
- เสร็จเมื่อ: หน้าจอเรียก API จำลอง แสดงช่วงเวลาและที่นั่งคงเหลือภายใน 30 วัน และโหลดช่วงเวลาใหม่เมื่อเปลี่ยนแพ็กเกจ
- สถานะ: พร้อมทำ

### T-14 สร้างหน้ายืนยันและแสดงช่วงทดแทน
- รองรับ: FR-BKG-03, FR-BKG-04
- ตรวจด้วย: AC-BKG-03
- ไฟล์ที่แตะ: `frontend/src/pages/ConfirmBooking.jsx`, `frontend/src/api/client.js`, `frontend/src/App.jsx`, `frontend/src/__tests__/AC-BKG-03.test.jsx`
- ต้องทำหลัง: T-13
- เสร็จเมื่อ: เมื่อ API จำลองตอบ 409 หน้าจอแสดงข้อความ "ช่วงเวลาเต็ม" และตัวเลือกช่วงว่าง 3 รายการโดยไม่แสดงผลสำเร็จเป็นการจอง
- สถานะ: พร้อมทำ

### T-15 สร้างหน้าผลลัพธ์การจอง
- รองรับ: FR-BKG-04, FR-BKG-05
- ตรวจด้วย: ไม่มี AC ตรง ๆ เป็นงานพื้นฐานของ T-18
- ไฟล์ที่แตะ: `frontend/src/pages/BookingResult.jsx`, `frontend/src/App.jsx`, `frontend/src/__tests__/BookingResult.test.jsx`
- ต้องทำหลัง: T-14
- เสร็จเมื่อ: หน้าจอแสดงผลการจองและหมายเลขคิวเมื่อได้รับผลสำเร็จ รวมถึงกรณีการส่งข้อความไม่สำเร็จตามสัญญา API โดยส่วนเลขคิวรอ Q-02
- สถานะ: รอ Q-02

### T-16 เชื่อมหน้าจอเข้ากับ API จริง
- รองรับ: FR-BKG-01, FR-BKG-03, FR-BKG-04, FR-BKG-05
- ตรวจด้วย: ไม่มี AC ตรง ๆ เป็นงานบูรณาการของ T-17 และ T-18
- ไฟล์ที่แตะ: `frontend/src/api/client.js`, `frontend/src/App.jsx`, `frontend/src/pages/SlotPicker.jsx`, `frontend/src/pages/ConfirmBooking.jsx`, `frontend/src/pages/BookingResult.jsx`
- ต้องทำหลัง: T-08, T-09, T-11, T-13, T-14, T-15
- เสร็จเมื่อ: หน้าจอเรียก endpoint จริงผ่าน `/api` และแสดงผลสำเร็จ, 409 ช่วงเต็ม และผลการส่งข้อความไม่สำเร็จได้ครบตามสัญญา API
- สถานะ: รอ Q-02

### T-17 ทดสอบการยืนยันการจองสำเร็จบนหน้าจอ
- รองรับ: FR-BKG-04
- ตรวจด้วย: AC-BKG-01
- ไฟล์ที่แตะ: `frontend/src/__tests__/AC-BKG-01.test.jsx`
- ต้องทำหลัง: T-15
- เสร็จเมื่อ: test ของ AC-BKG-01 ตรวจว่าหน้าจอแสดงผลการยืนยันและหมายเลขคิวจาก API จำลองตามคำตอบ Q-02
- สถานะ: รอ Q-02

### T-18 ทดสอบการส่งข้อความล้มเหลวบนหน้าจอ
- รองรับ: FR-BKG-05, NFR-REL-02
- ตรวจด้วย: AC-BKG-04
- ไฟล์ที่แตะ: `frontend/src/__tests__/AC-BKG-04.test.jsx`
- ต้องทำหลัง: T-15
- เสร็จเมื่อ: test ของ AC-BKG-04 ตรวจว่าหน้าจอยังแสดงหมายเลขคิวเมื่อ API จำลองระบุว่าส่งข้อความไม่สำเร็จ ตามคำตอบ Q-02
- สถานะ: รอ Q-02

### T-19 ทดสอบเวลาจองของผู้ใช้ใหม่
- รองรับ: NFR-USE-01, ASM-05
- ตรวจด้วย: ไม่มี AC ตรง ๆ เป็นงานตรวจคุณภาพการใช้งานตาม NFR-USE-01
- ไฟล์ที่แตะ: `frontend/src/__tests__/usability-booking.test.jsx`, `docs/srs/README.md`
- ต้องทำหลัง: T-16
- เสร็จเมื่อ: มีแผนทดสอบกับอาสาสมัครใหม่ 10 คน และบันทึกผลว่าผู้ทดสอบอย่างน้อย 8 คนจองสำเร็จภายใน 3 นาทีโดยไม่ขอความช่วยเหลือ
- สถานะ: พร้อมทำ

## ตารางตรวจความครบ

### Acceptance Criteria

| AC ID | task ที่ตรวจ AC นี้ |
|---|---|
| AC-BKG-01 | T-06, T-17 |
| AC-BKG-02 | T-07 |
| AC-BKG-03 | T-08, T-14 |
| AC-BKG-04 | T-09, T-18 |
| AC-BKG-05 | T-05 |
| AC-BKG-06 | T-10 |

### Constraints

| Constraint ID | task ที่ทำให้เป็นจริง |
|---|---|
| CON-TECH-01 | T-01 |
| DOM-PDPA-01 | T-01, T-10 |
| IF-IDP-01 | T-02, T-11 |
| IF-HIS-01 | T-01, T-03, T-06, T-11 |
| IF-NOT-01 | T-09 |

## สิ่งที่ยังไม่ทำ

- Q-02: หมายเลขคิวรีเซ็ตรายวันหรือนับต่อเนื่อง และมีรูปแบบอย่างไร เช่น `A001` โดยต้องถามเจ้าหน้าที่เวชระเบียน
- งานที่รอ Q-02: T-06, T-07, T-09, T-15, T-16, T-17 และ T-18
- จนกว่าจะได้คำตอบ Q-02 จะไม่กำหนดวิธีออกเลขคิว, รูปแบบเลขคิว, ขอบเขตการไม่ซ้ำ หรือการตรวจที่พึ่งพาหมายเลขคิว
